base_django_vagrant_puppet
==========================

A base Vagrant and Puppet confs and modules to use with a Django project


Change Vagrant file info, about the project name, and remove/add things from manifests/init.pp
